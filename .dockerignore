.venv
# Python 虚拟环境，几百 MB，绝对不能进镜像
__pycache__
# Python 缓存文件
chroma_db
# 向量数据库数据（用数据卷持久化，不进镜像）
.git
# git 历史记录
.idea
# PyCharm 配置
config.json
# ★ API key！不进镜像（用挂载卷从外部注入）
frontend
# 前端目录（本次只部署后端，整个排除，还能省构建时间）
*.db
# 数据库文件（用数据卷持久化）
.learnings
# 学习记录（你自己的笔记，不进镜像）
