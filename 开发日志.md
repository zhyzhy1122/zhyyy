# 宠物店 RAG 项目开发日志

---

## 2026-08-15

### 今日工作内容

#### 1. 对话历史管理（已完成）

- 新增 `services/database.py`，使用 SQLite 存储会话消息
  - 表 `chat_messages`：`id`、`session_id`、`role`、`content`、`created_at`
  - 函数：`init_db()` 建表 / `save_message()` 存消息 / `get_chat_history()` 取最近 N 条
  - 按 `session_id` 区分会话，默认会话 `"default"`
- `rag_service.py` 接入多轮对话
  - 在 `RAG_PROMPT` 中通过 `MessagesPlaceholder("{chat_history}")` 注入历史消息
  - `ask(question, session_id)` 先取历史 → 存用户问题 → 调链 → 存 AI 回答
  - 历史转为 `HumanMessage` / `AIMessage` 对象传给链

#### 2. 修复 chain 输入问题（关键 Bug）

- **问题**：调用 `rag_chain.invoke({"question": ..., "chat_history": [...]})` 传字典时，`RunnablePassthrough()` 会把整个字典原样传给 retriever，导致 `embed_query` 收到字典而非字符串，硅基流动 API 返回 400（错误码 20015）
- **解决**：改用标准库 `operator.itemgetter` 从输入字典中提取字段
  - `"context": itemgetter("question") | retriever | format_docs`
  - `"question": itemgetter("question")`
  - `"chat_history": itemgetter("chat_history")`
- 验证：命令行问答可正常返回"宠物护理服务"的完整回答

---

## 2026-08-14

### 今日工作内容

#### 1. 项目代码 LangChain 迁移

- **step1_load.py**：将原生文档加载和切分代码迁移到 LangChain
  - 使用 `langchain_core.documents.Document` 封装文档对象
  - 使用 `CharacterTextSplitter` 替代手写切分逻辑
  - 保留原有 JSONL 加载逻辑

- **step2_embed.py**：将原生 requests 调用迁移到 LangChain
  - 使用 `langchain_siliconflow.SiliconFlowEmbeddings` 封装硅基流动 API
  - 参数名：`siliconflow_api_key`、`model`、`base_url`（指向国内站 `.cn`）
  - 模型：`BAAI/bge-m3`，向量维度 1024

- **step3_store.py**：将原生 ChromaDB 操作迁移到 LangChain
  - 使用 `langchain_chroma.Chroma.from_documents()` 一行完成向量化+存储
  - 集合名：`pet_shop`，持久化路径：`./chroma_db`

- **step4_retrieve.py**：完成检索+生成，并重构为 LCEL 链式结构
  - 使用 `ChatPromptTemplate` 定义提示词模板（占位符：`{context}`、`{question}`）
  - 使用 LCEL 管道语法组装链：`retriever | format_docs` → `RAG_PROMPT` → `llm` → `StrOutputParser`
  - 模型：`deepseek-v4-flash`（DeepSeek V4 系列，旧模型名 `deepseek-chat` 已停用）

#### 2. 知识库数据

- 知识库文件：`knowledge_base/pet_shop_docs.jsonl`
- 共 75 篇文档，涵盖：服务项、套餐与价目、政策文档、宠物养护知识、高频FAQ
- 切分后约 80+ 个 chunk 存入 ChromaDB

#### 3. 已解决的技术问题

- `langchain_siliconflow` 默认请求国际站 `.com`，需手动指定 `base_url="https://api.siliconflow.cn/v1"`
- DeepSeek 旧模型名 `deepseek-chat` / `deepseek-reasoner` 已于 2026-07-24 停用，需使用 `deepseek-v4-flash` 或 `deepseek-v4-pro`
- `deepseek-v4-flash` 默认开启思考模式，`temperature` 参数不生效

---

### 后续开发规划

#### 前端方案
- 使用 FastAPI 自带 Swagger UI（`/docs`）作为接口测试页面
- 暂不开发独立前端页面

#### 后端改进计划（标注当前进度）

| 改进项 | 方案 | 说明 | 状态 |
|---|---|---|---|
| 对话历史管理 | SQLite 存储 | 支持多轮对话，记住上下文 | ✅ 已完成 |
| **文档管理** | **换成数据库** | **替代原有 JSONL 文件，支持动态增删改查** | **⏳ 下一步开发** |
| 文档 CRUD 接口 | RESTful API | GET/POST/PUT/DELETE + 重新向量化 | ⏳ 待开发 |
| 接口返回格式 | 结构化返回 | 同时返回 answer + sources（参考资料） | ⏳ 待开发 |
| 项目结构重组 | 拆分服务层 | 重组为 `api/`、`services/`、`models/` 目录结构 | ⏳ 待开发 |

#### 文档管理规划（当前重点）

目标：前端开发前先打通后端，让用户后续能通过界面**动态增删改查**知识库文档。

- **存储改造**：新增 SQLite 表 `documents` 存文档元数据（id、title、content、created_at、updated_at），替代 JSONL 文件
- **向量同步**：文档增删改后，同步更新 ChromaDB 向量（新增 → 向量化入库；删除 → 按 doc_id 删除；修改 → 删除旧 chunk 后重新向量化）
- **RESTful 接口**：
  - `GET /documents` 列出全部文档
  - `GET /documents/{id}` 查看单篇
  - `POST /documents` 新增文档（自动向量化）
  - `PUT /documents/{id}` 修改文档（重新向量化）
  - `DELETE /documents/{id}` 删除文档（同步删向量）
- **检索联动**：`document_service.py` 的切分逻辑在文档变动时复用，保证 RAG 检索到的是最新知识
- 完成后通过 Swagger UI 测试，再开发前端

#### 预计项目结构

```
项目目录/
├── main.py              # FastAPI 入口
├── api/                 # 接口定义
│   ├── chat.py          # 问答接口
│   └── documents.py     # 文档管理接口
├── services/            # 业务逻辑
│   ├── rag_service.py   # RAG 检索+生成
│   └── document_service.py  # 文档管理
├── models/              # 数据模型
│   └── schemas.py       # 请求/响应格式定义
├── knowledge_base/      # 知识库（已有）
└── chroma_db/           # 向量库（已有）
```

---

### 技术栈确认

| 组件 | 技术选型 |
|---|---|
| 向量 Embedding | 硅基流动 BAAI/bge-m3 |
| 向量数据库 | ChromaDB |
| LLM | DeepSeek V4 Flash |
| RAG 框架 | LangChain + LCEL |
| 后端框架 | FastAPI |
| 对话历史存储 | SQLite |
| 文档管理存储 | 数据库（待定具体方案） |
